package dragons.init

/**
 * Properties related to the initialization of the game. Currently, just the parameters that were passed to the main
 * method.
 */
class GameInitProperties(val mainParameters: MainParameters)
